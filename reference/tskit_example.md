# Create deterministic example tree-sequence data

The example contains two sample nodes, one ancestor, one tree over a
sequence of length 10, one site, and one mutation. It exercises the
native constructor without external software.

## Usage

``` r
tskit_example()
```

## Value

An in-memory `TreeSequence`.
