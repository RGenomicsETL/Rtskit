# Traverse tree intervals

Traverse tree intervals

## Usage

``` r
tskit_trees(x)
```

## Arguments

- x:

  A `TreeSequence`.

## Value

A data frame containing each tree index, half-open interval, root count,
and active edge count.
