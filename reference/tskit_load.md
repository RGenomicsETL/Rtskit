# Load a succinct tree sequence

Load a succinct tree sequence

## Usage

``` r
tskit_load(path)
```

## Arguments

- path:

  Existing `.trees` file.

## Value

A `TreeSequence` owning the loaded native value.
