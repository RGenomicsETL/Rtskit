# Materialize the edge table

Metadata remains native and is not decoded by this initial accessor.

## Usage

``` r
tskit_edges(x)
```

## Arguments

- x:

  A `TreeSequence`.

## Value

A data frame with zero-based IDs and edge coordinates and nodes.
